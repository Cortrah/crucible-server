'use strict';

// 'create-table' rules numActors => table (&game)
module.exports = {
    method: 'POST',
    path: '/api/create-table',
    options: {
        handler: async (request, h) => {}
    }
};
